# Deviations log（预注册之后的全部代码改动）

预注册：OSF https://osf.io/usycb（2026-09-19 02:23:34），引用 freeze r4 `d207c263df938f6003e51ff3da90f13367b40d99`（README 填 hash 的提交 `471f0561`）。
规则：此后任何代码 / 配置改动都记在这里，带 commit hash、原因、是否影响预注册的分析或判定。

| # | 日期 | commit | 改动 | 原因 | 对预注册的影响 |
|---|---|---|---|---|---|
| D1 | 2026-09-19 | `832ec52` | `setup/sync_cloud.sh`：默认目标改为新 pod `root@195.26.233.96:27805`；push 规则放行 `results/whitelist_extra_banned.json` 与 `results/whitelist_audit.md`，并排除 `archive/` | RunPod 自动迁移换了机器（/workspace 迁移后为空，需重推重装）；原 push 规则排除整个 `results/`，会让 pod 缺少 287 个 id 的审计禁集 → 白名单变成 8638 / 8742（与预注册的 8351 / 8455 不符） | 无（基础设施；保证 pod 上的白名单与预注册一致） |
| D2 | 2026-09-19 | `347ab06` | `scripts/10_check_lora_embed_sync.py`：设置 RANK / LOCAL_RANK / WORLD_SIZE / MASTER_ADDR / MASTER_PORT（与 TRL 0.25.1 colocate 相同）；每个变体改为单独子进程；删掉未使用的 untied 变体入口 | 首次在 GPU 上运行即 `KeyError: 'RANK'`（vLLM external_launcher 需要分布式环境变量）；一个进程内多次建引擎会重复初始化进程组 | 无（验证脚本；判定阈值与被测同步逻辑 `trl_sync` 未改） |
| D3 | 2026-09-19 | `ee45149` | `scripts/10_check_lora_embed_sync.py`：追加诊断量 noise_floor（vLLM 基座 vs HF 基座）、delta_agreement / delta_corr（两个引擎各自看到的改动量是否一致）；加两条任务风格的 prompt；**原判定 `ok`（max Δlogp < 0.05）不变** | 首次运行三个变体（含只挂 q_proj 的对照）都不满足 0.05：max 0.22 来自跨引擎 bf16 噪声（对照同步后 mean 0.035，q+embed 同步后 mean 0.031），阈值无法区分“没同步”与“噪声” | 无代码路径影响；embed LoRA 的去留由用户依据两套数字决定（`cot_compress/lora.py` 的门仍读原 `ok`） |
| D4 | 2026-09-19 | `7fd0ec0` | `scripts/10_check_lora_embed_sync.py`：追加 true_noise_floor（vLLM 基座 vs HF `disable_adapter`）、sampler_vs_trainer（同步后的 vLLM vs 训练端“adapter 激活未合并”前向）、hf_merged_vs_trainer_forward | D3 的“HF 基座”实为 adapter 激活未合并的前向（= 训练端算 logp 的前向）；要判断的量是采样端与训练端是否同分布，而不只是权重有没有拷进 vLLM | 无代码路径影响；用于决定 embed LoRA 去留 |
| D5 | 2026-09-19 | `a4cd14c` | `configs/cloud_4b.yaml`：`train.lora_embed_lm_head: false` —— **全臂去掉 embed_tokens LoRA**，LoRA 目标 = attention + MLP（q/k/v/o/gate/up/down），r=32 α=64 不变 | `scripts/10_check_lora_embed_sync.py` 在 A100 / vLLM 0.10.2 / TRL 0.25.1 / peft 0.21.0 上未通过：权重确实拷进了 vLLM（同步后 vLLM vs HF 合并 = 噪声底 mean 0.033），但 Qwen3-4B tied embeddings 下，TRL 的 merge→同步把 embed 的增量写进共享张量，采样端在输出头也带上了增量，而训练端（同步后 unmerge、adapter 激活未合并）的前向只在输入嵌入处有：采样端 vs 训练端 max / mean \|Δlogp\| = 0.965 / 0.186（只挂 q_proj 的对照 0.208 / 0.031；纯跨引擎噪声底 0.19–0.25 / 0.035）。原判定 `ok`（max<0.05）对三个变体都为 False（该阈值低于噪声底，对照也不过）；结论依据 D4 的 sampler_vs_trainer | 属预注册写明的门控分支（“不通过 → 全臂去掉 embed LoRA 并记日志”）；对所有臂一致，不影响臂间比较；臂 B 少了嵌入层的可训练自由度（可能更难冷启动，B_warm 条件分支不变） |
| D6 | 2026-09-19 | `b556f62` | `configs/cloud_4b.yaml`：`generation.max_new_tokens_direct: 32`（原继承值 16）；`scripts/01_calibrate.py`：新增 `--direct-only`（只重测直接作答并写回准入 json、重算判定），准入 json 多记 direct_format_err / max_new_tokens_direct | 首格准入发现 direct=0.000 且没有任何可解析的直接答案（pairwise_direct=None）：n=8 的答案要 18 个 token（数字与空格各一个 + `</answer>` 3 个），n=9 要 20 个，16 的上限在 `</answer>` 前截断，所有直接作答都判格式错 | **影响判定量**：第 1 条（direct ≤ 2×2^−d）、第 5 / 6 条（相对 direct）、训练中残留检查的冻结 direct 锚点都依赖 direct。修复后才是预注册所指的量；原生 / 预算 / 填充 / 移植 / letter-ban 各项不受影响。(8,4,5) 格在修复前已开始，用 `--direct-only` 重测其 direct；其余格直接用修复后的配置 |
| D7 | 2026-09-19 | `1c86938` | 新增 `scripts/pod_stop.sh`：封装 `runpodctl pod stop <id>`（Stop，不 Terminate）；pod id 取自 pod 自身环境；默认在还有 GPU 计算进程时拒绝停机（`--force` 覆盖）；`--dry-run` 只核对；`--delay` 延时；停机前往 cloud_log 记一行。API key 不在仓库里（在 pod 的 /workspace/.runpod） | 用户要求配置自动停机，长任务收尾统一调用，避免 GPU 空转计费 | 无（基础设施） |
| D8 | 2026-09-19 | `1a6ecc4` | `configs/cloud_4b.yaml`：cap 5120 → **10240**（`train.max_completion_length`、`generation.max_new_tokens_think`），`model.max_seq_length` 6144 → 11264；`scripts/train.py`：`reward.m_override`（仅 `--dry-run` / `--smoke` 允许，吞吐测量用的临时 M，正式 M 仍来自该 cap 下的准入） | cap 5120 下冻结 Qwen3-4B 的原生轨迹绝大多数被截断：(8,4,5) 强制收尾 91.6%、(8,4,6) 94.2%，自然结束的准确率 100% / 96.6%；(8,4,5) 在 cap 8192 下自然结束仍只有 43%（准确率 97.7%）、中位数仍顶在 cap。第 2 条不过完全由截断造成 | **改变预注册的训练 / 评估上限**：M（奖励里的长度归一化）、强制预算曲线的尺度、每步 token 与成本都随之改变；对所有臂一致。准入在新 cap 下整体重跑 |
| D9 | 2026-09-19 | `1a6ecc4` | `cot_compress/shortcuts.py`：guess_verify 改为**定义 C**——≥3 个带验证词的完整顺序（原条件）**且**存在一对相邻、顺序不同的此类候选，二者之间没有任何传播 / 分情况标记；同一顺序的重述与逐条复核不算。新增 `reasoning_markers()`。其余三个捷径检测器不变 | 原定义在 (8,4,5) 的冻结原生轨迹上命中 140/500，其中 104 条答对、42 条自然结束的轨迹里命中 41 条：它标的是“找到顺序后逐条复核”，不是“无推理地连猜多个候选”。无标记的间隔 79% 两侧是同一个顺序的重述。定义 C 下 140 条里只有 8 条（全 500 条的 1.6%）属于换候选而无推理 | **影响准入第 7 条**（捷径命中 <5%）以及各臂收敛后轨迹的捷径报告。该定义是在看过冻结模型的原生轨迹之后、任何训练之前改的（数据依赖的改动，如实记录）；两种定义下的命中率都会在准入表里并报 |
| D10 | 2026-09-19 | `1a6ecc4` | `cot_compress/admission.py`：第 4 条只在 native 曲线该预算点 > chance + 10pp 时比较填充与 native | (8,4,5) / (8,4,6) 的第 4 条不过都发生在 0.5M：native 0.054 / 0.022、填充 0.002 / 0.000，两者都≈0，“不进 10pp 内”空成立地失败，与填充是否有用无关（冻结前我标过这一行为） | **影响准入第 4 条**：去掉空成立的失败；在 native 明显高于 chance 的预算点上判据不变 |
| D11 | 2026-09-19 | `cc11427` | `scripts/train.py`：把 `train.vllm_enable_sleep_mode`（默认 false）透传给 GRPOConfig；吞吐测试用命令行覆盖 `train.vllm_gpu_memory_utilization=0.6`、`train.dry_run_steps=5`（**仓库里的默认配置未改**） | 用户要求的配置调优：cap 10240 下 vLLM 0.45 时 KV 只装 199k token（并发 18.5 条 < 每步 32 条），训练侧峰值 74.4 / 78.4 GiB 已贴顶；micro-batch 本来就是 1（GA 32），只升 vLLM 占比会 OOM，需要 sleep mode 才能腾出显存 | 无（吞吐调优，不改任何规则 / 奖励 / 批大小）。注意：若正式采用 sleep mode，训练中 eval 走 trainer.llm 前需要先 wake_up，届时另记 |
| D12 | 2026-09-19 | `7cb710c` | `cot_compress/shortcuts.py`：perm_enum 的"≥8 个不同完整顺序"一支改为只计入**无推理切换**里的顺序——相邻两个不同的完整顺序之间没有任何传播 / 分情况标记时，这一对的两个顺序才计入（`unreasoned_orders()`，标记集与 D9 相同）；关键词各支（n!、n! 的数值、all permutations、itertools）不变。`detect_shortcuts` / `shortcut_rate` 加 `legacy_pe`，准入并报 r4 原定义。新增 `scripts/15_recompute_criterion7.py`：用已存原生轨迹重算第 7 条（不重跑生成，其余指标原样保留） | cap 10240 下 (8,4,5) 的 perm_enum 命中 74/500，其中 73 条由"≥8 个不同完整顺序"触发、62 条是被截断的长轨迹：长轨迹在带推理的搜索里累计试过 ≥8 个候选，并非无推理枚举。与 D9 同理 | **影响准入第 7 条**以及各臂收敛后轨迹的捷径报告。看过冻结模型原生轨迹之后、任何训练之前的数据依赖改动，如实记录；r4 原定义与"仅 D9"的命中率继续并报。**重算结果：perm_enum 14.8% → 0.6%，总命中 25.8% → 13.2%，第 7 条仍不过**（guess_verify 7.2%、ir_copy_order 4.2%、assign_enum 2.0%） |
| D13 | 2026-09-19 | `919bfae` | **准入第 7 条改为人工审计判定**：`cot_compress/admission.py` 第 7 条 = `audit_template_rate`（原生轨迹里"模板 / 抄答案"的比例）< 5%，未审计 → pending；check 键改名 `7_template_audit_lt_5pct`。四个自动捷径检测器**降为描述量**，r4 / D9 / D12 / D13 四种定义的命中率并报（`shortcuts.VERSIONS`、`shortcut_rates_all`，准入 json 的 `shortcut_rates`）。修两个检测器 bug（只影响第四个描述量 D13）：guess_verify 的候选里排除恒等排列与事件清点；ir_copy_order 要求最后一处抄写片段与最终顺序之间无传播 / 分情况标记。`scripts/15_recompute_criterion7.py` 加 `--audit-rate / --audit-file`。审计表 `results/audit_criterion7_ord_n8_h4_d5.md`。`setup/sync_cloud.sh`：默认目标改为迁移后的 pod（195.26.233.65:20967），push 放行审计表与正式准入 json。README 第 7 条处加指向本条的注记 | D12 之后第 7 条仍为 13.2%。逐条读完被标中的 66 条：0 条是模板 / 抄答案，50 条是重述约束后的正常推理，16 条是推理失败后摆候选；guess_verify 32 条里 16 条由事件清点 `The events are 0,1,…,7` 这个恒等排列触发，ir_copy_order 20 条全部是核对清单 / 开头复述 / 回读题面，assign_enum 全部由 `either way / both cases` 口头语触发。自动检测器量的不是预注册想量的东西 | **改变准入第 7 条的判定方式**（自动检测器 → 人工审计），属看过冻结模型原生轨迹之后、任何训练之前的数据依赖改动。审计范围 = 被 D12 任一检测器标中的 66 条，未标中的 434 条未读；审计人是编码代理（Claude），用户审阅前 52 条分类表后决定。四种定义的检测器命中率（0.922 / 0.258 / 0.132 / 0.060）都报。结果：audit_template_rate = 0/500 → **(8,4,5)@cap 10240 八条全过，准入通过，M = 8599.5**（`results/admission_ord_n8_h4_d5.json` 入库） |
| D14 | 2026-09-19 | `1e0e160` | 预算决定：**只跑矩阵前三项** A1 → B_warm-SFT(A1) → B1（warm 判定），到此停机。新增 `configs/matrix_stage1.yaml`（= matrix_core 前三项，A1 的 20 步测速不重做）、`scripts/chain_stage1.sh`（pod 上的自动链，结束写 `results/CHAIN_DONE` 后 `pod_stop.sh --delay 300`）；`scripts/run_matrix.py` 加 `--strict`：操纵门失败 / kill、任何子进程非零退出（含 OOM）、残留检查触发（`uninterpretable.json`）、SFT 源缺失都立刻停（exit 4，写 `results/matrix_halt.json`），**不进入 λ / G 自动重调** | cap 10240 下 200 步 × 11 run 投影约 $571，超过写死的 $300 预算；用户决定先跑到 warm 判定为止再看 | **偏离预注册的矩阵执行方式**：门失败时预注册写的是自动重调 λ=0.3 → 1.0 → G=32，本阶段改为停机汇报、由用户决定；矩阵其余项（B2–4、A2–3、A″、C_rand、B_warm-RL）本阶段不跑。训练 / 奖励 / 评估 / 停止判据 / 门的阈值都没改。warm 判定仍按原代码在 B1 结束后用其 ≤200 步的 eval 计算（数值与"到 200 步时判"相同） |
| D15 | 2026-09-19 | `c48c601` | 论文草稿 `docs/paper.md` 进仓库（用户手动放入，execution 会话不改其内容）；新增 `docs/roles.md`：本仓库有 execution（跑 GPU / 改代码 / 管 pod）与 planning（只读，写 `docs/paper.md` 与分析）两个 Claude Code 会话，planning 不碰代码和 pod，execution 不改 `docs/paper.md`，冲突以用户为准 | 便于双会话协作 | 无（文档；不影响协议、代码或任何判定） |
| D16 | 2026-09-20 | `2c914d8` | `scripts/train.py`：`train.dry_run_eval_n`（默认 0 = 关；只在 `--dry-run` 下生效）——dry-run 训练前先走一次小 eval（think 与 direct 各 n 题，与正式 run 的 step-0 eval 同一条代码路径，之后不再 eval）。`.gitignore` 加 `*:Zone.Identifier` | 排查 A1 在第 1 个训练步 backward 的 `CUDA error: invalid argument`（迁移后的宿主机，driver 595.91.07）：需要区分"宿主机 / 驱动问题"与"eval 之后接训练"两种根因；原 dry-run 不带 eval，无法复现后者 | 无（排查开关，默认关闭；正式 run 的代码路径不变） |
| D17 | 2026-09-19 | `e9edfc1` | 会话分工改为单会话：planning-led 于 2026-09-19 23:50 UTC 接管全部操作（`docs/roles.md` 追加一节；`ops/state.md` 记接管时间与常设规则；`ops/to_execution.md` 存档；`ops/dashboard.py` = 只读 `cloud_pull/` 的本地看板，只显示已记录数字与预注册阈值线、不产生判定）。同一提交带入重叠期积压的 `docs/paper.md` 改动：七处与现行协议对齐（§4.4 cap [D8]、§4.2 第 7 条 [D13]、§4.2 第 4 条 [D10]、§4.3 LoRA [D5]、§4.4 门失败停机 [D14]、§4.1 / §4.2 原生长度改实测、§7.4 措辞），逐处行内标注 deviation 并保留预注册原文；§6.1 / §6.2 成本部分填数；§9 决策史。`.gitignore` 加看板生成物 | execution 的链结束（A1 崩停）后用户指示接管；论文文本此前落后于 D5–D14 | 无（文档 / 分工 / 只读工具）。没有改任何判定标准；paper.md 里的每处改动都对应一条已记录的 deviation |
| D18 | 2026-09-19 | `c6467df` | `configs/cloud_4b.yaml`：`cost.usd_per_hour` 1.9 → **1.6** | pod 实际单价 $1.59/h；用户定预算线按 $1.6/h 判；cloud_log 的 $571 投影原本就按 1.6 算，配置里的 1.9 是旧值 | 只影响美元投影：`run_matrix.budget_projection`（B_warm-RL 前的预算检查）与 `--dry-run` 报告。改前改后：同样 GPU·h 下投影金额 × 1.6/1.9 = 0.842（例：357 GPU·h → $678 → $571）。预注册写死的是 $300 上限（未动），不是费率；不改任何规则 / 阈值 / 训练 / 评估 |
| D19 | 2026-09-20 | `6fb0817` | `setup/auto_pull.sh`：每 600 s 固定拉一次曲线文件（steps / eval / designated_ckpt / uninterpretable / matrix_log / matrix_halt / CHAIN_DONE / results 的 json；不含 `*.zst`、`reward_log.jsonl`、checkpoint），原有的变化触发完整 pull 不变。新增 `ops/host_check.py`（宿主机 backward 阶梯检查，不走 TRL、不 import 项目代码）、`ops/train_anomaly.py`（anomaly 模式包装器，原样执行 train.py）；`ops/dashboard.py` 加“排查 / 验收 run”一块 | 原 watcher 只在 eval（每 50 步 ≈ 7 h）变化时 pull，用户无法看到链是否活着；A1 首步 backward 间歇性崩溃的排查需要不改训练代码的工具 | 无（本地基础设施与只读 / 排查工具；不改任何规则、判定、训练、评估；拉取频率不是预注册页写死的内容） |
| D20 | 2026-09-20 | `e97320e` | **A1 首步 backward 崩溃的修复**：① `configs/cloud_4b.yaml`：`train.vllm_gpu_memory_utilization` 0.45 → **0.35**（先定 0.40 并验收，用户随后定 0.35：0.40 的约 2 GiB 余量对 400 步 / 27 h 的碎片增长太薄，`--strict` 下崩一次要人工开机；0.35 的改动在提交 `95c1686`）；② `setup/env_cloud.sh`：vLLM torch.compile / inductor / triton 三处编译缓存指到持久盘 `/workspace/.cache/`（`VLLM_CACHE_ROOT`、`TORCHINDUCTOR_CACHE_DIR`、`TRITON_CACHE_DIR`）；③ `scripts/chain_stage1.sh`：run_matrix 之前先跑一个可丢弃的 1 步 dry-run 预热（退出码不计）。`setup/auto_pull.sh` 另拉 `logs/*.rc` / `*.start`（看板据此显示崩溃 / 进行中），`ops/dashboard.py` 相应改版 | A1 正式 run 与两次对照在第 1 个训练步 backward 报 `CUDA error: invalid argument`。2×2 对照（这台机器）：全冷编译 × util 0.45 崩 3/3；全冷 × 0.40 过；热 / 半冷缓存 × 0.45 过 3/3、× 0.40 过 6/6。C++ 栈：LogsumexpBackward → 申请张量 → CUDACachingAllocator::malloc → release_block → invalid argument，即显存顶到上限时分配器回收缓存块的路径出错；0.45 下训练侧峰值 reserved 78.4 / 79.25 GiB。容器盘上的编译缓存每次 pod stop 清空，所以每次开机后的第一个进程都是全冷 | **不影响预注册的任何规则 / 判定 / 训练超参 / 评估**：显存划分与缓存位置不在预注册页上；批大小 2×16、cap 10240、reward、LoRA、eval 划分 / n / 温度 / 频率均未动，step-0 eval 照常。改前改后（实测，三档）：util 0.45 → KV 199,296 token / 并发 18.5 / 494 s 每步 / 训练侧峰值 reserved 78.4 GiB；0.40 → 170,448 / 15.9 / 503 s / 77.3 GiB；**0.35（采用）→ 141,248 / 13.1 / ≈503 s（494–508，单步样本）/ 72.9 GiB**（余量约 6.4 GiB）；投影成本随步时约 +2%。采样分布不变（同一模型、同一采样参数）；注意新编译的 kernel 会使采样在数值上不逐位相同（全冷与热缓存的同 seed batch 不同），这在修复前后都一样。验收：util 0.35 下全冷启动连过 3 次、其中一次带小 eval（acc4，2026-09-20 05:32 UTC）；对照：全冷 × 0.45 崩 3/3。详见 cloud_log |
| D21 | 2026-09-20 | `dff63fc` | 新增 `scripts/pod_watchdog.sh`：pod 上常驻的兜底停机——GPU 连续空转 ≥ 90 min，或 `/workspace/logs/DECISION_PENDING` 存在且超过 90 min（需用户拍板的决策的倒计时），即调用 `pod_stop.sh --force`（Stop 非 Terminate） | 用户夜间无人值守：要求需其过目的决策出现后倒计时 1.5 h、未回复即停机，且不空转超过 1.5 h | 无（基础设施；不改任何规则 / 判定 / 训练 / 评估。停机只是中断执行：checkpoint 每 25 步、`run_matrix` 自动 `--resume`，恢复后 run 继续，不影响任何预注册的量） |
| D22 | 2026-09-20 | `94c5a08` | **门失败后的处理按失败模式重新分派**（在 step-100 eval 数据到来之前定；仅改"门失败后做什么"的分派，门本身的三项阈值、奖励、批大小、cap、eval 划分一律不动）：**(0)** 若收敛停止触发得早而 L 曲线仍在下行（触发时 L 相对 step-0 降幅 < 30%，且最近三个 eval 点的 L 单调下降）→ 判为**停止判据过松**，不是长度压力不足：收紧停止判据（**ΔL < 3% 且 Δacc < 3pp**，连续两个区间不变；由用户于 2026-09-20 20:55 UTC 定，此时 A1 在 step 92、仍只有 step-0 / step-50 两次 eval，step-100 数据尚未产生。另一备选「保持 5%/4pp 但要求三个连续小区间」已废弃：它只推迟不改'平'的定义，三个各降约 4% 的区间累计仍降约 12%，照样会在曲线下降中触发），**对所有臂一律适用**，并**从当前 checkpoint 继续 A1、不重跑**；**(1)** 若 L 曲线已平、降幅仍 < 30%，且 acc 损失 ≤ 5pp、L_median ≥ 2×Kahn（真"压不动"）→ **跳过 λ=0.3，直接 λ=1.0**，重跑 A1；**(2)** 若 acc 损失 > 5pp 或 L_median < 2×Kahn（压过头）→ 按注册顺序 **λ=0.3**；**(3)** λ=1.0 仍不过 → **G=32**（注册顺序不变） | 注册的重调顺序以 λ=0.3 打头，而 λ=0.3 是比现行 0.5 **更弱**的长度压力（`r = 10·[c·(1 − min(λL/M, 0.9)) − …]`，λ 越大压力越强）。这与协议自身的两处记载矛盾：§9（2026-09-16）记录「臂 A λ=0.3 终止于 step 150……六个 eval 点显示 L 中位 1820→1742，在噪声内」即 λ=0.3 长度信号过弱；README §2.6 又写「kill = 压到底了没有空间，**重调 = 压不动**，两者互斥」——若重调分支是给"压不动"用的，以更弱的 λ 打头是反方向。每次重调 = 重跑整个 A1（约 65 h ≈ $105），先试一个有据可查会更弱的设置代价过高。(0) 另外单列，是因为 A1 的停止判据最早可在 step 100 触发（step-0 eval 计入 hist），而实测 0→50 区间 \|ΔL\|/L = 3.93%（门槛 5%）已算"小"，存在"曲线仍在下行却被判收敛"的具体风险；该情形的证据指向停止判据过松，与长度压力无关 | **不改门的判据本身**（≥30% 减少、≤5pp acc 损失、≥2×Kahn 下限、kill = ≤1.5×决策下限 均不变），只改门失败后走哪条分支、以及分支 (0) 新增。(0) 若触发：停止判据改动**对所有臂一律适用**（§3.4 要求臂间共享停止规则，故同仪器性质保持），A1 从 checkpoint 继续而非重跑（不引入新的初始化 / 数据顺序）；改前定义 = 连续两个 50 步区间 ΔL_mean < 5% 且 Δacc < 4pp，改后定义与实测数值在触发时记入本表。(1)–(3) 只改注册序列的**顺序**，不新增也不删除任何设置；未被走到的设置（如 λ=0.3）在论文中如实说明未被尝试及原因。本条在任何 step-100 数据出现之前写定 |
| D23 | 2026-09-22 | `PENDING23` | `scripts/run_matrix.py`：工作区可写性探测的循环变量 `for d in (runs, samples, results)` 改名为 `_probe_dir` | **变量遮蔽 bug**：该循环把第 141 行的 `d = m.get("defaults", {})`（矩阵默认配置字典）覆盖成 `PosixPath`。第一个 run 结束后 `d` 已坏，队列取下一个 run 时 `d.get("config", …)` 抛 `AttributeError: 'PosixPath' object has no attribute 'get'`，`run_matrix` rc=1 退出。实况：2026-09-22 10:01 UTC，A1 正常收敛（step 350）、诊断 rc=0、**操纵门已判定通过**（日志已写入 `results/matrix_log.md`）之后，转 B_warm-SFT 时崩溃；`chain_stage1.sh` 据 rc=1 写了 `CHAIN_DONE` 并启动 `pod_stop --delay 300`（已在倒计时内取消，pod 未停）。该 bug 只在**第二个及以后**的 run 触发，此前所有测试都是单 run，故未暴露 | 无（纯代码 bug 修复；不改任何规则 / 判定 / 阈值 / 奖励 / 训练 / 评估）。A1 的结果与门的判定均在崩溃之前完成、未受影响：`train_rc=0 diag_rc=0`，门 `{"ok": true, "kill": false, "token_reduction": 0.36, "acc_loss": -0.308, "L_median_final": 4539.5}`。回归验证：修复后 `run_matrix --matrix configs/matrix_stage1.yaml --dry-run` 可走完全部三个 run 而不再抛异常。重启链时 A1 因 `final/` 已存在被跳过，不重跑、不重判门 |
