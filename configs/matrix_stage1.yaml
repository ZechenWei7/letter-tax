# D14（2026-09-19，预算决定）：只跑矩阵的前三项 A1 → B_warm-SFT(A1) → B1（warm 判定），到此停机，不进入矩阵其余部分。
# 与 matrix_core.yaml 前三项相同；A1 的 20 步测速已单独做过（results/dry_run_A_ord_n8_h4_d5_cap10240.json），不重做。配 run_matrix.py --strict 使用。
defaults: {config: configs/cloud_4b.yaml, steps: 400}
runs:
  - {key: ord_n8_h4_d5, arm: A,     lam: 0.5, seed: 1, priority: 99}
  - {key: ord_n8_h4_d5, arm: Bwarm_sft, seed: 1, priority: 99, source: A_seed}
  - {key: ord_n8_h4_d5, arm: B,     lam: 0.5, seed: 1, priority: 99, warm_check: true}
